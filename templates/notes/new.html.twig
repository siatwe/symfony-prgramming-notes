{%  extends 'base.html.twig' %}
{% block title %}Index{% endblock %}
{% block body %}
    <div class="container">
        <h1>Create Note</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        <a href="{{ path('notes_index') }}">back</a>
    </div>
{% endblock %}
